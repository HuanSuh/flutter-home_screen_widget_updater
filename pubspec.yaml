name: home_screen_widget_updater
description: A new Flutter plugin.
version: 0.0.4

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    androidPackage: doodle.fac.home_screen_widget_updater
    pluginClass: HomeScreenWidgetUpdaterPlugin
